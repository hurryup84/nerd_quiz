---
name: node-version-requirement
description: Node.js 22 is required for proper environment
metadata:
  type: project
---

The project requires Node.js 22 to run properly. Use `nvm use 22` to ensure the correct Node version before running npm commands. The setup.sh and start.sh scripts already handle this automatically.